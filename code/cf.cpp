#include <bits/stdc++.h>

void solve() {
    int a, b;
    std::cin >> a >> b;
    std::cout << a + b << "\n";
}

int main() {
    std::cin.tie(nullptr)->sync_with_stdio(false);

    int t;
    std::cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}
